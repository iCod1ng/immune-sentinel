spring:
  thymeleaf:
    cache: false
  datasource:
    url: jdbc:mysql://localhost:3306/immune_sentinel?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&allowPublicKeyRetrieval=true
    username: root
    password: root

sentinel:
  base-url: http://localhost:8080
  token-secret: dev-secret-do-not-use-in-prod
  storage:
    local-dir: ./data/uploads
